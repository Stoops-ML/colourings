Installation
============

Requirements
------------

* Python 3.10+

Install from PyPI
-----------------

.. code-block:: bash

   pip install colourings

Install from source
-------------------

.. code-block:: bash

   git clone https://github.com/Stoops-ML/colourings.git
   cd colourings
   pip install -e .
